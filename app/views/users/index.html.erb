<h1>All the users!</h1>
<ul>
<% @users.each do |user| %>
  <li>
      <%= user.full_name %>
      <%= user.email %>
      <%= link_to "Videos", user_videos_path(user.id) %>
  </li>
<% end %>
</ul>

<br />
<%= link_to 'Create User', new_user_path %>
<br />
